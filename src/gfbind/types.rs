// use std::collections::HashMap;

// excluded for now, just use JsValue
// pub enum Type {
//     Void,
//     Any(HashMap<String, Type>), // unknown
//     Boolean(bool),
//     Number(f64),
//     String(String),

//     ByteBuffer(Vec<u8>),
//     UintBuffer(Vec<u32>),
//     IntBuffer(Vec<i32>),
//     FloatBuffer(Vec<f32>),
//     DoubleBuffer(Vec<f64>),

//     Promise(Box<Type>),
//     List(Vec<Box<Type>>),
//     Object(HashMap<String, Type>),

//     // MultiPoint3(DoubleBuffer),
//     // Mesh(MultiPoint3, UntBuffer),
// } 